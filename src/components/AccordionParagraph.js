import React from "react";
import "../styles/Accordion.css";

const AccordionParagraph = ({ className, body }) => {
  return <div className={className}>{body}</div>;
};

export default AccordionParagraph;
